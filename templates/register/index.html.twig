{% extends 'base.html.twig' %}

{% block title %}Register!{% endblock %}
{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-8 offset-2">
            <div class="form-group">
                <fieldset>
                {{form_start(form)}}
                    {{ form_widget(form) }}
                {{form_end(form)}}
                </fieldset>
            </div>
        </div>
    </div>
</div>
{% endblock %}
